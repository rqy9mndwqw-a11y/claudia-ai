# DISABLED — Deploying manually via `npx wrangler deploy --env production`
# This workflow exists as reference only and does not run.
#
# name: Deploy to Cloudflare Workers
# on:
#   push:
#     branches: [main]
